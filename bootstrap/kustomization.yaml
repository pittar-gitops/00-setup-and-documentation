# Assumes the cluster admin had pre-created the sealed-secrets project and secret.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
- https://github.com/pittar-gitops/01-admin-cluster-config.git/overlays/00-argocd
resources:
- 00-cluster-config-proj.yaml
- 01-sealed-secrets-app.yaml
- 02-role-anyuid.yaml
- sealed-secrets-namespace.yaml
- sealed-secrets-secret.yaml
